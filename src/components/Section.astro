---
// Se ejecuta en el servidor o en el build time <-----
const { title } = Astro.props;
---

<section class={title === "Skills" ? "no-print" : "print"}>
  {title && <h2>{title}</h2>}
  <slot />
</section>

<script>
  // Se ejecuta en el cliente
</script>

<style>
  section {
    max-width: 700px;
    margin: 0 auto 24px;
  }

  h2 {
    margin-bottom: 8px;
    font-weight: 700;
    line-height: 1.5;
    font-size: 1.5rem;
  }

  @media print {
    .no-print {
      display: none !important;
    }
  }
</style>
